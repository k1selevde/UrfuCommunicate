let initialState = {
    user: {
        isAuth: false,
        id: null,
        token: null
    },
    errorMsg: '',
    isLoading: false
}

export default (state = initialState, action) => {
    return state;
}

